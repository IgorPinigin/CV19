<Window x:Class="CV19.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:map="clr-namespace:MapControl;assembly=MapControl.WPF"
        Title="XAML MapControl - Projection Demo" Height="1050" Width="1920" WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <DockPanel>
        <map:Map MaxZoomLevel="22" ZoomLevel="12" Center="55,83" MapLayer="{Binding CurrentLayer}"
                 MouseRightButtonUp="Map_MouseRightButtonUp">
            <map:MapBase.MapProjection>
                <Binding Path="CurrentProjection">
                    <Binding.FallbackValue>
                        <map:WebMercatorProjection/>
                    </Binding.FallbackValue>
                </Binding>
            </map:MapBase.MapProjection>

            <map:Pushpin map:MapPanel.Location="{Binding PushpinLocation}"
                         Content="{Binding PushpinText}"/>
        </map:Map>
    </DockPanel>
</Window>